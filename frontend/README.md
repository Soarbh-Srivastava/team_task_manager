# Team Task Manager — Frontend

Minimal Vite + React + TypeScript scaffold for the Team Task Manager frontend.

Quick start

```bash
cd frontend
npm install
npm run dev
```

Environment

Set `VITE_API_BASE_URL` to point to your backend (default: http://localhost:3000).
